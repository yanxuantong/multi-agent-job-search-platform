[project]
name = "jobagent"
version = "0.1.0"
description = "Reference multi-agent job-search platform for learning orchestration, HITL, evals, and tracing."
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
langgraph = [
  "langgraph",
  "langgraph-checkpoint-postgres",
]
llm = [
  "anthropic",
  "openai",
]
observability = [
  "langfuse",
]
postgres = [
  "psycopg[binary]",
  "pgvector",
]
mcp = [
  "mcp",
]
dev = [
  "pytest",
]
all = [
  "anthropic",
  "langfuse",
  "langgraph",
  "langgraph-checkpoint-postgres",
  "mcp",
  "openai",
  "pgvector",
  "psycopg[binary]",
]

[project.scripts]
jobagent = "jobagent.cli:main"

[tool.pytest.ini_options]
pythonpath = ["."]
