services:
  - type: web
    name: jobagent-demo
    runtime: docker
    plan: free
    autoDeploy: true
    healthCheckPath: /healthz
    envVars:
      - key: JOBAGENT_PUBLIC_DEMO_MODE
        value: "true"
